import './Card.css';
const Card = () => {
    const randomNum = Math.floor(Math.random() * 6)
    
    return (
        <div className="card">
            
        </div>
    );
};

export default Card;