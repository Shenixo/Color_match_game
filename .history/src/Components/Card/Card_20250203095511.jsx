import './Card.css';
const Card = () => {
    const randomNum = Math.floor(Math.random() * 6)
    const card
    console.log(randomNum)
    return (
        <div className="card">
            
        </div>
    );
};

export default Card;