import './Card.css';
export const randomNum = Math.floor(Math.random() * 6)
const Card = () => {
    console.log(randomNum)
    return (
        <div className="card">
            
        </div>
    );
};

export  Car{d;